<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Braid - Desktop - Join</title>

    <style type="text/css">
body {
  font-family: "Open Sans", Helvetica, Arial, sans-serif;
  font-size: 12px;
  margin: 0;
  padding: 0;
  background: #F2F2F2;
  height: 100%;
  overflow: hidden;
}

body textarea, body input {
  font-family: inherit;
  font-size: 1em;
}
 @font-face {
  font-family: FontAwesome;
  src: local('FontAwesome'), url('https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/fonts/fontawesome-webfont.eot?#iefix&v=4.7.0') format('embedded-opentype'), url('https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/fonts/fontawesome-webfont.woff2?v=4.7.0') format('woff2'), url('https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/fonts/fontawesome-webfont.woff?v=4.7.0') format('woff'), url('https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/fonts/fontawesome-webfont.ttf?v=4.7.0') format('truetype'), url('https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/fonts/fontawesome-webfont.svg?v=4.7.0#fontawesomeregular') format('svg');
  font-weight: normal;
  font-style: normal;
}
.app >.main >.header {
  position: absolute;
  top: 1rem;
  left: 4rem;
  margin-left: 1rem;
  right: 1rem;
  display: flex;
  justify-content: space-between;
  z-index: 102;
}

.app >.main >.header >.group-header >.bar {
  display: inline-block;
  vertical-align: top;
}

.app >.main >.header >.group-header >.bar {
  background: black;
  color: white;
  height: 2rem;
  border-radius: 3px;
  overflow: hidden;
  box-shadow: 0 1px 2px 0 #ccc;
}


.app >.main >.header >.group-header >.bar >.group-name {
  text-transform: uppercase;
  letter-spacing: 0.1em;
  font-weight: bold;
  line-height: 2rem;
  -webkit-font-smoothing: antialiased;
  padding: 0 0.75rem;
  min-width: 5em;
}

.app >.main >.sidebar {
  width: 4rem;
  top: 0;
  overflow-y: auto;
  padding: 1rem;
  flex-direction: column;
  -webkit-flex-direction: column;
  position: fixed;
  z-index: 9999;
  overflow-x: visible;
  box-sizing: border-box;
  display: flex;
  bottom: 0;
  background: #222;
  left: 0;
}

.app >.main .gateway {
  min-height: 100vh;
  font-family: Open Sans;
  margin: 0;
  line-height: 1.5;
  background: #f3f3f3;
  color: #444444;
}

.app >.main .gateway input, .app >.main .gateway button {
  font-family: Open Sans;
}

.app >.main .gateway >h1.header {
  color: #2bb8ba;
  font-weight: normal;
  font-size: 1.75em;
  text-align: center;
  margin: 2rem 0;
}

.app >.main .gateway >h1.header::before {
  width: 1.25em;
  height: 1.25em;
  margin-right: 0.5em;
  margin-bottom: -0.15em;
  content: "";
  background-size: contain;
  margin-left: -1.75em;
  display: inline-block;
  background-image: url(/images/braid-logo-color.svg);
}

.app >.main .gateway >.section-wrapper{
  text-align: center;
}

.app >.main .gateway >.section-wrapper >.section {
  background: #ffffff;
  display: inline-block;
  border: 1px solid #e0e0e0;
  padding: 2rem;
  margin: 0 auto;
  box-sizing: border-box;
  margin-bottom: 2rem;
  border-radius: 3px;
}

.app >.main .gateway >.section-wrapper >.section >form >fieldset {
  margin: 0;
  padding: 0;
  border: none;
}

.app >.main .gateway >.section-wrapper >.section >form >fieldset >h1 {
  margin: 0 0 1.5rem;
  color: #2bb8ba;
  font-size: 1.2em;
}

.app >.main .gateway >.section-wrapper >.section >form >fieldset >.option {
  margin: 0 0 2rem;
  position: relative;
}

.app >.main .gateway >.section-wrapper >.section >form >fieldset >.option >h2 {
  font-size: 1em;
  color: #444444;
  margin: 0 0 0.5rem 0;
}
.app > .main .gateway >.section-wrapper >.section >form >fieldset > label {
  display: block;
  margin-bottom: 2em;
}

.app > .main .gateway >.section-wrapper >.section >form >fieldset > label > input[type=text],
.app > .main .gateway >.section-wrapper >.section >form >fieldset > label > input[type=email],
.app > .main .gateway >.section-wrapper >.section >form >fieldset > label > input[type=password] {
  font-size: 1.25rem;
  font-family: Open Sans;
  padding: 0 0.4em;
  width: 100%;
  box-sizing: border-box;
  border: 1px solid #e0e0e0;
  line-height: 2em;
  height: 2em;
  border-radius: 3px;
}

.app > .main .gateway >.section-wrapper >.section >form >fieldset >label >input:focus {
   border-color: #2bb8ba;
   outline: none;
}

.app > .main .gateway >.section-wrapper >.section >form >fieldset >label >input::-webkit-input-placeholder,
.app > .main .gateway >.section-wrapper >.section >form >fieldset >label >input::-moz-placeholder {
   color: #444444;
}

.app > .main .gateway >.section-wrapper > .section > form > fieldset > label > h2 {
  font-size: 1em;
  color: #444444;
  margin: 0 0 0.5rem 0;
  text-align: left;
  margin-bottom: 0;
}

.app >.main .gateway button.submit {
  width: 100%;
  justify-content: space-between;
  border: none;
  text-transform: uppercase;
  align-items: center;
  -webkit-align-items: center;
  -webkit-font-smoothing: antialiased;
  padding: 1rem;
  text-align: left;
  white-space: nowrap;
  cursor: pointer;
  font-weight: bold;
  font-size: 1.25em;
  letter-spacing: 0.05em;
  border-radius: 3px;
  display: flex;
  transition: background 0.2s ease-in-out;
  -webkit-transition: background 0.2s ease-in-out;
  background: #2bb8ba;
  color: white;
}

.app >.main .gateway button.submit >.group-name {
  max-width: 25vw;
  overflow: hidden;
  text-overflow: ellipsis;
}

.app >.main .gateway button.submit::after {
  font-family: fontawesome;
  content: "";
}

.app >.main .gateway button.submit:hover {
  background: #2e9394;
}

.app >.main .gateway button.submit:active {
  background: #185556;
}

.app >.main .gateway button.submit:focus {
  outline: none;
}

.app >.main .gateway button.submit.disabled {
  background: #cccccc;
  cursor: not-allowed;
}
    </style>

  </head>
  <body>
    <div class="app">
      <div class="main">
        <div class="sidebar"></div>
        <div class="header">
          <div class="group-header">
            <div class="bar"><div class="group-name">{{group-name}}</div></div>
          </div>
        </div>


        <div id="join-and-register">
          <div class="gateway">
            <h1 class="header">Welcome to Braid</h1>
            <div class="section-wrapper">
              <div class="section user-auth">
                <h2>Join the group {{group-name}}</h2>
                <form action="/api/link-register" method="POST">
                  <input type="hidden" name="group-id" value="{{group-id}}">
                  <input type="hidden" name="now" value="{{now}}">
                  <input type="hidden" name="form-hmac" value="{{form-hmac}}">
                  <fieldset>
                    <label>
                      <h2 class="field">Email</h2>
                      <input type="email" name="email" placeholder="email address">
                    </label>
                    <label>
                      <h2 class="field">Password</h2>
                      <input type="password" name="password" placeholder="•••••••••••">
                    </label>
                    <button class="submit" type="submit">Register</button>
                  </fieldset>
                </form>
                <p>If you already have an account <a href="/">Log in</a> first, then come back here</p>
              </div>
            </div>
          </div>
        </div>

        <div id="join" style="display: none;">
          <div class="gateway">
            <h1 class="header">Welcome Back to Braid</h1>
            <div class="section-wrapper">
              <div class="section user-auth">
                <form action="/api/link-join" method="POST">
                  <input type="hidden" name="now" value="{{now}}">
                  <input type="hidden" name="form-hmac" value="{{form-hmac}}">
                  <input type="hidden" name="group-id" value="{{group-id}}">
                  <fieldset>
                    <button class="submit" type="submit">Join the "<span class="group-name">{{group-name}}</span>" group</button>
                  </fieldset>
                </form>
              </div>
            </div>
          </div>
        </div>

      </div>
    </div>
    <script type="text/javascript">
      fetch(`/api/session`,
            {credentials: "include"})
        .then(r => {
          if (r.ok) {
            document.getElementById("join").style.display = "block";
            document.getElementById("join-and-register").style.display = "none";
          }
        })
    </script>
  </body>
</html>
